INSERT OR REPLACE INTO GlobalParameters (Name, Value)
SELECT ('CAPITAL_IDENTITY_PRESSURE_TILE_RADIUS_MAXIUMUM_BASE'), ('8');

INSERT OR REPLACE INTO GlobalParameters (Name, Value)
SELECT ('CITIZEN_IDENTITY_PRESSURE_RADIUS_CUTOFF'), ('8');

INSERT OR REPLACE INTO GlobalParameters (Name, Value)
SELECT ('LOYALTY_PER_TURN_FROM_NEARBY_CITIZEN_PRESSURE_MAX_LOYALTY'), ('16');